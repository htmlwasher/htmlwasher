.venv/
data/
datasets/
*.parquet
*.csv
__pycache__/
.pytest_cache/
.ruff_cache/

# Lockfiles are not committed (repo convention). deps live in requirements.txt,
# so this is a virtual stub that pins nothing.
uv.lock

# train.py now writes model.xgb.json + tfidf-vocab.json directly into
# packages/htmlwasher/native/artifacts/ (committed, include_str!-ed by the crate);
# there are no training-local artifact copies to ignore.
